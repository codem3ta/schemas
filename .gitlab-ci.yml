stages:
  - deploy

deploy_staging:
  stage: deploy
  image: node:alpine
  before_script:
    - apk add --no-cache aws-cli
    - node deploy.js
  script:
    - aws s3 cp ./ s3://$S3_STAGING_BUCKET --recursive --exclude "*" --include "*.json"
  only:
    - main
